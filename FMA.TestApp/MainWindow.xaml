<Window x:Class="FMA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:FMA.View;assembly=FMA.View"
        Title="MainWindow"
        Width="860"
        Height="530" >
    <Window.Resources>
        <DataTemplate DataType="{x:Type view:FlyerMakerViewModel}">
            <view:FlyerMakerView />
        </DataTemplate>
    </Window.Resources>

    <ContentControl Content="{Binding FlyerMakerViewModel}" />

    <!--<view:FlyerMakerView x:Name="FlyerMakerView" HorizontalAlignment="Stretch" />-->

</Window>
